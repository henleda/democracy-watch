{
  "name": "@democracy-watch/api",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild src/members.ts --bundle --platform=node --target=node20 --outdir=dist --external:pg-native",
    "build:types": "tsc --emitDeclarationOnly",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@democracy-watch/shared": "workspace:*",
    "@aws-sdk/client-secrets-manager": "^3.712.0",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.147",
    "@types/node": "^22.10.7",
    "@types/pg": "^8.11.10",
    "esbuild": "^0.27.2",
    "typescript": "^5.7.3"
  }
}
